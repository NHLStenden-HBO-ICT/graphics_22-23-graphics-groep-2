package main.maths;

public class Constants {
    //EPSILON is an arbitrary very small number we can use as an error margin
    //or offset when casting shadow rays to prevent shadow acne
    public static final double EPSILON = 0.0000001;
}
